# FewShotTest
Some toy experiments on few shot segmentation

# Method 1 (Evaluate Now)
Few-shot segmentation (FSS) paradigm method: use one DINOv2 encoder fine-tuned on other dataset (ACDC/AMOS) with self-supervised training to encode the bone MRI set. 
